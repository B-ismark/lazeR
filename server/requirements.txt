pynput>=1.7.6
cryptography>=42.0.0   # AES-256-GCM secure wire (encrypt + authenticate packets)
zeroconf>=0.131.0      # LAN auto-discovery (mDNS)
qrcode>=7.4.2          # QR code generation
Pillow>=10.0.0         # QR image rendering in GUI
pystray>=0.19.0        # minimize-to-tray (close keeps it running)
# Windows-only, needed for VOL command:
pycaw>=20230407 ; sys_platform == "win32"
comtypes>=1.2.0 ; sys_platform == "win32"
