# Actions are pinned to commit SHAs (a moved tag can't change what runs while the
# signing key is on disk); Dependabot proposes the bumps as reviewable PRs.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
